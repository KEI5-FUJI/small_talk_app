<% provide(:title, "フォロー中ユーザーの依頼") %>
<%= render 'layouts/header' %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1>
          <%= current_user.name %>
        </h1>
      </section>
      <section class="stats">
        <%= render "shared/stats" %>
      </section>
      <section class="task_form">
        <%= render 'tasks/task_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>フォロー中のユーザーの依頼</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
